enablePlugins(ScalaJSPlugin)

name := "Canvas"

version := "0.1-SNAPSHOT"

scalaVersion := "2.12.8"

//The following sbt setting turns your Scala.js project into an “application” rather than a “library”
scalaJSUseMainModuleInitializer := true
